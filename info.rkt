#lang setup/infotab

(define collection 'multi)

(define deps (list "base"
                   "compatibility-lib"
                   "rackunit-lib"))

(define build-deps (list "scribble-lib"
                         "net-doc"
                         "racket-doc"))
